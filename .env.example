# API
PORT=8787
CORS_ORIGIN=http://localhost:5173

# OpenAI (optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# When true, API returns stable mock results and never calls the model.
MOCK_MODE=true

